<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.opiniones">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Opiniones" >

        <activity android:name="com.example.opiniones.Main"
            android:label="main"
            android:exported="true">

            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER"/>
                <action android:name="android.intent.action.MAIN"/>
            </intent-filter>

        </activity>
        <activity android:name="com.example.opiniones.Detalle"
            android:label="detalle"/>
        <activity android:name="com.example.opiniones.Edit"
            android:label="edit"/>
        <activity android:name="com.example.opiniones.ListadoLOpis"
            android:label="listadoLOpis"/>

    </application>

</manifest>